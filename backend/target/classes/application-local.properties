# Copy this file to application-local.properties (which is ignored by Git),
# then replace every value before running the application.
spring.datasource.url=jdbc:mysql://localhost:3306/autoflowsrilanka_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Lojan@2003

# Base64-encoded, random key of at least 32 bytes.
jwt.secret=bXktc3VwZXItc2VjcmV0LWtleS1mb3ItYXV0b2Zsb3ctc3JpbGFua2EtYmFja2VuZA==

# Required to allow initial administrator registration.
app.registration.admin-secret=local-admin-secret-123

# Include each browser origin that may make authenticated requests.
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173

